/*
export interface ITaskService {
  create(); // create(rDate: Omit<IAction, "id">): Promise<IResult<IAction>>
  findAll(); // : Promise<IResult<IAction[]>>;
  findByName(); // findByName(name: string): Promise<IResult<IAction[]>>;
  findById(); // findById(id: string): Promise<IResult<IAction>>;
  useAction(); // useAction(id: string, resellerToken: string, args: any): Promise<IResult<any>>;  ;
  delete(); // delete(id: string): Promise<IResult<{}>>;
  update(); // update(id: string, data: Partial<IAction>): Promise<IResult<{}>>;
} // fazer funcionar a longo prazo

*/
